# Server Configuration
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/watchparty

# JWT Secret
JWT_SECRET=your_jwt_secret_key_change_this_in_production

# CORS
CLIENT_URL=http://localhost:3000

# Session
SESSION_SECRET=your_session_secret_change_this_in_production

# OAuth Credentials (Add your streaming service OAuth credentials)
# Netflix (Example - Netflix doesn't have public API, use for reference)
NETFLIX_CLIENT_ID=
NETFLIX_CLIENT_SECRET=

# Add other streaming services as needed
